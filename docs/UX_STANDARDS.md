# UX Standards

The interface mimics classic desktop aesthetics using `system.css`. Panels are draggable windows with clear labels. Inputs validate user data and provide alerts on errors. Controls are keyboard accessible and responsive.
