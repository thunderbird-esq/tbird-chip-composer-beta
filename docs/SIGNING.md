# Commit Signing

Contributors are encouraged to sign their commits using GPG or SSH keys. Signed
commits help verify authorship and improve trust in the repository history. To
sign a commit with GPG:

```bash
git commit -S -m "Your message"
```
