# Practice Drills

New contributors can get familiar with the codebase by tackling small tasks:

* Add a unit test for a simple function in `src/`.
* Fix a linter warning or minor bug.
* Update documentation and run the build script.
