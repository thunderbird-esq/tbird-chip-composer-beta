# Contribution Checklist

* Run `npm test` to ensure unit tests pass.
* Execute `bash build.sh` and confirm it completes with no errors.
* Verify that the application works in a modern browser by opening `index.html` via a local web server.
* Follow the code style described in `CODE_MANIFEST.md` when submitting patches.
