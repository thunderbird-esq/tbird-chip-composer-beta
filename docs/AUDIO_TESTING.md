# Audio Testing

Automated tests for the audio engine run via Node. Execute `npm test` to run the
suite. For interactive testing open `test-runner.html` in a browser and review
the console output for failures.
