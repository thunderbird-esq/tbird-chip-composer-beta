# Build Automation

The `build.sh` script copies assets to the `dist/` directory and bundles the
JavaScript using `esbuild` if available. Run it before packaging or deploying:

```bash
bash build.sh
```
