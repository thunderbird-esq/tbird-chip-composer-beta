# Developer Onboarding

1. Clone the repository and run `npm install` to fetch dev dependencies.
2. Use `npm test` to verify the environment.
3. Launch a local server with `npx http-server` or `python3 -m http.server`.
4. Start hacking on the modules under `src/`.
