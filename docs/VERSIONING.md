# Versioning Policy

The project follows [Semantic Versioning](https://semver.org/). Increment the
major version for breaking changes, the minor version for new features, and the
patch version for bug fixes.
